PER-REQUISITES FOR WORKING IN REPO:

    Recent version of NodeJS installed
    Recent version of npm installed

To Launch App on localhost:

> Navigate to "diagonal-counting-game" directory
> run "npm install" in terminal to install dependencies
> run "npm run dev" to run the app
